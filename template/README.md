# Forge MDK Template Placeholder

Copy your Minecraft Forge MDK contents into this directory before using the generator.

Minimum required files checked by backend:

- `build.gradle`
- `gradlew`

You should also include:

- `gradlew.bat`
- `gradle/wrapper/*`
- `settings.gradle`
- default `src/main/resources` with `mods.toml` / metadata
